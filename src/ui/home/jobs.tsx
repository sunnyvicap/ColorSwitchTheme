import { SafeAreaView, Text } from "react-native";

export default function Jobs() {

    return (
        < Text style = {{
            alignContent: 'center',
            alignItems : 'center'
        }}> JOBS</Text >
    )

}