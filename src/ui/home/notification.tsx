import { SafeAreaView, Text } from "react-native";

export default function Notification() {

    return (
        < Text style = {{
            alignContent: 'center',
            alignItems : 'center'
        }}> Notifcation</Text >
    )

}